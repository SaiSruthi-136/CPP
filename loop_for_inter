#include <iostream>
#include <string>
using namespace std;

int main() {
    int a, b;
    if (!(cin >> a >> b)) return 0;   // read input safely

    string numbers[] = {
        "one","two","three","four","five",
        "six","seven","eight","nine"
    };

    for (int i = a; i <= b; ++i) {
        if (i >= 1 && i <= 9) {
            cout << numbers[i - 1] << '\n';
        } else {
            cout << (i % 2 == 0 ? "even" : "odd") << '\n';
        }
    }

    return 0;
}
